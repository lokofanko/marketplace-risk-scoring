name: ML Service CI

on:
  push:
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print working directory
        run: pwd

      - name: Set up Python
        uses: actions/setup-python@v5
         with:
          python-version: "3.11"

      - name: List repository files
        run: ls -la

      - name: Print hello
        run: echo "ML Service CI started"